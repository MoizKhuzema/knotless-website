---
/**
 * /about
 *
 * Rebuilt on the system /what-we-do established: a hero that holds the screen
 * on its own, sections opened by the measured thread, the shared type ladder
 * with its short-viewport step-down, the cursor-light variants, and the one
 * reversed panel to close.
 *
 * What went with the old page: the typewriter H1, the eyebrow, the hero CTA,
 * the ember dot markers, the twelve-column career rows, the founder bios, and
 * the commented-out portrait band. Three sections now.
 *
 * Each section takes the shape its own content asks for, which is the other
 * half of that system — not the same block three times:
 *
 *   1 · the hero      the whole argument: two observations side by side and the
 *                     conclusion across both, which is the shape of the claim
 *   2 · delivery      the claim at statement size, the mechanics beneath it
 *   3 · the close     the page's single reversed panel
 *
 * Copy lives in src/data/about.ts; founder names and titles in site.ts.
 */
import BaseLayout from '../layouts/BaseLayout.astro';
import CtaButton from '../components/CtaButton.astro';
import { graph, webPage, founderPersons } from '../lib/schema';
import { ABOUT_META, ABOUT_HERO, ABOUT_DELIVER, ABOUT_CLOSE } from '../data/about';

const origin = Astro.site!.origin;
const pageUrl = new URL(Astro.url.pathname, Astro.site).href;

/* A Person node per founder, plus this WebPage. The page names both founders in
   its prose, so the structured data still has something to describe. */
const schema = graph([
  webPage(origin, { url: pageUrl, name: 'About Us', description: ABOUT_META.description }),
  ...founderPersons(origin),
]);
---

<BaseLayout title="About Us" description={ABOUT_META.description} schema={schema}>
  {/* --- Section 1 — hero ------------------------------------------------ */}
  {/* No opening thread: the thread marks a section within a page, and the top
      of a page is not one.

      Top-aligned, like /what-we-do's. It was weighted to the bottom while it
      held two elements — a headline and a standfirst pinned to the top of a
      screen with 60% of it empty underneath read as a page that had not
      finished loading. It now carries the whole argument, so there is nothing
      to weight: the content fills the screen and the air goes above it. */}
  <section class="ahero" data-reveal>
    {/* Broken where the sentence turns, not where the box runs out. Six words
        wrapping on their own read as a paragraph that happens to be large; two
        deliberate lines read as a headline. */}
    <h1 class="ahero__h r" style="--i: 0">
      <span>{ABOUT_HERO.heading.lead}</span>{' '}<span>{ABOUT_HERO.heading.tail}</span>
    </h1>

    {/* Each observation under its own rule, then one rule across both and the
        conclusion under that. The rules are what make the change of tone
        legible: dim, dim, then cream reads as an accident when three paragraphs
        simply follow each other, and as an argument when a line separates the
        evidence from what it adds up to. */}
    <div class="ahero__careers">
      {
        ABOUT_HERO.observations.map((observation, i) => (
          <div class="ahero__col r" style={`--i: ${i + 1}`}>
            <span class="ahero__rule" data-catch="line" aria-hidden="true" />
            <p class="ahero__obs">{observation}</p>
          </div>
        ))
      }
    </div>

    <div class="ahero__end r" style="--i: 3">
      <span class="ahero__rule ahero__rule--full" data-catch="line" aria-hidden="true"></span>
      <p class="ahero__conclusion">{ABOUT_HERO.conclusion}</p>
    </div>
  </section>

  {/* --- Section 2 — how we deliver --------------------------------------- */}
  <section class="sect sect--first">
    <div data-reveal>
      <span class="rule-thread r" data-catch="line" aria-hidden="true" style="--i: 0"></span>
      <h2 class="h-section sect__h r" style="--i: 1">{ABOUT_DELIVER.heading}</h2>
    </div>

    {/* The claim at statement size, the mechanics under it — the same shape the
        Assessment's deliverables take, and for the same reason: the sentence
        that commits to something should not be the same size as the sentence
        that explains how it works. */}
    <div class="deliver" data-reveal>
      <p class="deliver__statement r" style="--i: 0">{ABOUT_DELIVER.statement}</p>

      {/* The mechanics in glass — the page's only surface, and the site's own
          material rather than a new one. It does two things ninety words of
          prose on a bare page could not: it says where the explanation starts
          and stops, and it gives the page a middle register between the bare
          type above it and the cream slab below. */}
      <div class="deliver__pane r" data-catch="ring" style="--i: 1">
        <p class="deliver__body">{ABOUT_DELIVER.body}</p>
        <span class="deliver__rule" data-catch="line" aria-hidden="true"></span>
        <p class="deliver__closer">{ABOUT_DELIVER.closer}</p>
      </div>
    </div>
  </section>

  {/* --- Section 3 — the close -------------------------------------------- */}
  <section class="sect">
    <div class="aclose" data-reveal>
      <div class="aclose__inner">
        <span class="rule-thread aclose__rule r" data-catch="line" aria-hidden="true" style="--i: 0"
        ></span>
        <h2 class="h-section aclose__h r" style="--i: 1">{ABOUT_CLOSE.heading}</h2>
        <p class="aclose__body r" style="--i: 2">{ABOUT_CLOSE.body}</p>
        <div class="aclose__cta r" style="--i: 3">
          <CtaButton href={ABOUT_CLOSE.cta.href} variant="inverse">{ABOUT_CLOSE.cta.label}</CtaButton>
        </div>
      </div>
    </div>
  </section>
</BaseLayout>

<style>
  /* --- Section rhythm, shared with /what-we-do ---------------------------- */
  .sect {
    padding-top: var(--spacing-section-lg);
  }
  /* The first section after the hero clears less. The hero already ends on its
     own floor of screen, so the full section rhythm on top of that put 150px of
     nothing between the conclusion and the next heading — a gap that reads as a
     missing section rather than as a break. */
  .sect--first {
    padding-top: var(--spacing-section);
  }
  .sect__h {
    margin: 1.25rem 0 0;
  }

  /* --- Section 1 — hero -------------------------------------------------- */
  /* The full screen and nothing else on it: 100svh MINUS main's own clearance,
     because this section starts below that padding and a full 100svh would put
     its floor one clearance below the fold. Both come from --main-clear, so they
     cannot drift apart.

     svh, deliberately, not lvh or dvh. svh is the viewport with the browser
     chrome showing, which is the state a page loads in; lvh would make the hero
     taller than the screen at load, and dvh would resize it as the chrome
     retracts, moving the words while they are being read. */
  .ahero {
    display: flex;
    flex-direction: column;
    justify-content: flex-start;
    gap: clamp(1.75rem, 4vh, 3rem);
    min-height: calc(100svh - var(--main-clear));
    padding-bottom: clamp(1rem, 2vh, 3rem);
  }
  /* The air above the headline is spent only where there is screen to spend it,
     as on /what-we-do: on a phone or any short viewport it would come straight
     out of the conclusion at the bottom. */
  @media (min-width: 48rem) and (min-height: 46rem) {
    .ahero {
      padding-top: clamp(2rem, 7vh, 5rem);
    }
  }
  @media (min-width: 48rem) and (min-height: 50rem) {
    .ahero {
      padding-top: clamp(6rem, 17vh, 13rem);
      gap: clamp(2.25rem, 4.5vh, 4rem);
    }
  }
  /* Below 34rem of height the composition stops being a composition and starts
     pushing the headline into the bar. */
  @media (max-height: 34rem) {
    .ahero {
      min-height: 0;
      gap: clamp(1.25rem, 3vh, 2rem);
    }
  }
  .ahero__h {
    margin: 0;
    /* 80px at 1440, up from 72, and set as two lines rather than balanced into
       them — the size and the leading are doing the work that the word count
       will not. */
    font-size: clamp(2.5rem, 1.35rem + 4.3vw, 5rem);
    /* 0.96, not 1.02: two short lines of display type want to sit as a block,
       and the gap between them was reading as the space between paragraphs. */
    line-height: 0.96;
    letter-spacing: var(--tracking-display);
    font-weight: 600;
    color: var(--color-cream);
  }
  /* The break is the markup's, not the box's. Each half on its own line, so the
     headline reads the same at 1440 and at 390 instead of rewrapping into
     whatever the column allows. */
  .ahero__h span {
    display: block;
  }
  /* The two observations, side by side where there is width for it. Below 64rem
     they stack: measured, a half column at 768 ran 34 characters a line, which
     is a newspaper column for prose written as paragraphs. */
  .ahero__careers {
    display: grid;
    gap: 1.5rem;
  }
  @media (min-width: 64rem) {
    .ahero__careers {
      grid-template-columns: repeat(2, minmax(0, 1fr));
      gap: var(--grid-cell);
    }
  }
  /* An element, not a border, so it catches the cursor light like every other
     rule on the site. */
  .ahero__rule {
    display: block;
    height: 1px;
    background-color: var(--color-line);
  }
  .ahero__end {
    margin-top: clamp(1.75rem, 3.5vh, 3rem);
  }
  .ahero__end .ahero__conclusion {
    margin-top: 1.1rem;
  }
  .ahero__obs {
    margin: 1.1rem 0 0;
    font-size: clamp(1rem, 0.9rem + 0.45vw, 1.125rem);
    line-height: 1.6;
    color: var(--color-cream-dim);
    /* 52ch — the site's prose measure, and calibrated rather than assumed: a ch
       is the width of a ZERO, which in Inter is 0.63em, while the average
       character is nearer 0.5em. */
    max-width: 52ch;
  }
  /* The conclusion: cream, a step up, across the width both observations sat
     over. The turn from what each of us saw to what we did about it is carried
     by tone and size, so it needs no heading to announce it. */
  .ahero__conclusion {
    margin: 0;
    font-size: clamp(1.0625rem, 0.95rem + 0.55vw, 1.3125rem);
    max-width: 52ch;
    line-height: 1.5;
    letter-spacing: var(--tracking-tight);
    color: var(--color-cream);
    max-width: 52ch;
  }
  /* A laptop is a WIDE screen and a SHORT one — a 1440x900 display is a 1440x760
     viewport once the browser takes its chrome — so the type steps down by
     HEIGHT, which is the constraint that actually binds there. */
  @media (max-height: 50rem) {
    .ahero__h {
      font-size: clamp(2.25rem, 1.6rem + 2vw, 3.25rem);
    }
    .ahero__obs {
      font-size: clamp(0.9375rem, 0.9rem + 0.25vw, 1.0625rem);
    }
    .ahero__conclusion {
      font-size: clamp(1rem, 0.95rem + 0.3vw, 1.1875rem);
    }
  }

  /* --- Section 2 — how we deliver ---------------------------------------- */
  .deliver {
    margin-top: clamp(2rem, 3vw, 3rem);
  }
  .deliver__statement {
    margin: 0;
    /* 32px over the body's 21 — the same pair the Assessment's heading and its
       statements use. */
    font-size: clamp(1.5rem, 1.2rem + 1vw, 2rem);
    line-height: 1.2;
    letter-spacing: var(--tracking-display);
    font-weight: 600;
    color: var(--color-cream);
    max-width: 20ch;
  }
  /* The page's one surface: low-alpha fill, hairline edge, specular top line,
     blur, and no drop shadow — the same pane /what-we-do sets its quotes in. A
     shadow is what makes the nav read as floating, and this sits IN the page. */
  .deliver__pane {
    position: relative;
    margin-top: clamp(1.5rem, 3vw, 2.25rem);
    padding: clamp(1.5rem, 3vw, 2.5rem);
    border-radius: var(--radius-lg);
    background-color: var(--color-glass-soft);
    backdrop-filter: blur(14px) saturate(140%);
    -webkit-backdrop-filter: blur(14px) saturate(140%);
    border: 1px solid var(--color-glass-soft-edge);
    box-shadow: inset 0 1px 0 var(--color-glass-soft-spec);
    max-width: 62ch;
  }
  .deliver__body {
    margin: 0;
    font-size: clamp(1rem, 0.9rem + 0.45vw, 1.125rem);
    line-height: 1.65;
    color: var(--color-cream-dim);
  }
  .deliver__rule {
    display: block;
    height: 1px;
    margin: clamp(1.1rem, 2vw, 1.5rem) 0;
    background-color: var(--color-line);
  }
  /* The commitment the paragraph ends on, set as the pane's last word: cream, a
     step up, and on its own line. */
  .deliver__closer {
    margin: 0;
    font-size: clamp(1.0625rem, 0.95rem + 0.55vw, 1.3125rem);
    line-height: 1.45;
    letter-spacing: var(--tracking-tight);
    color: var(--color-cream);
  }

  /* --- Section 3 — the close --------------------------------------------- */
  .aclose {
    background-color: var(--color-invert-bg);
    color: var(--color-invert-fg);
    border-radius: var(--radius-lg);
    padding: clamp(1.75rem, 5vw, 6rem);
    position: relative;
  }
  .aclose__inner {
    position: relative;
  }
  /* Ink on paper, keeping .rule-thread's tick pattern so it takes the shared
     line variant and catches the light like every other rule. */
  .aclose__rule {
    color: var(--color-invert-line);
  }
  .aclose__h {
    margin: 1.25rem 0 0;
    color: var(--color-invert-fg);
  }
  .aclose__body {
    margin: 1.5rem 0 0;
    font-size: var(--text-lead);
    line-height: 1.5;
    letter-spacing: var(--tracking-tight);
    color: var(--color-invert-fg-dim);
    max-width: 52ch;
  }
  .aclose__cta {
    margin-top: clamp(2.5rem, 4vw, 3.25rem);
  }
</style>
